{
  "version": "1.9.0",
  "passed": 15,
  "environment": "Offline Chromium DOM + mocked HTTP/storage and simulated Web Locks",
  "orion_device_test": false,
  "checks": [
    "Core 24h completion schedules 2\u20135min rest; Fast alone does not skip it; Start now advances 7d",
    "Explicit collapse leaves one status-pill row; heartbeat does not reopen the body",
    "Export diagnostics.json downloads structured metadata and request/checkpoint log without token/body",
    "Every override path, including Rebuild and Fast, preserves active shared 429 cooldown",
    "Retry and Start now cannot erase schema/auth/storage hard-stop state",
    "Stage order is 24h core \u2192 24h Projects before 7d core",
    "After the stage rest is skipped/elapsed, 7d core begins",
    "Projects HTTP500 leaves explicit optional coverage gap but does not block core 24h/7d/30d",
    "Actual Retry errors button makes one request, increments attempts and reports why work stopped",
    "Rapid repeated manual retries are coalesced/rate-gated rather than hammering the failed source",
    "5 consecutive Project failures open a shared 30-minute source circuit; stages do not duplicate probes",
    "Recovered Projects source fills missing coverage without re-fetching already indexed unchanged chats",
    "Ring buffer is bounded to 200 allowlisted metadata events",
    "Stale pre-fix Project500 checkpoint is safely requeued with cached data retained",
    "Default older stages are slower; shared failure health slows even a Fast override"
  ]
}
