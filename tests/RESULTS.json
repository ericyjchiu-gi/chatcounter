{
  "version": "1.7.1",
  "passed": 23,
  "environment": "Chromium / mocked extension storage + ChatGPT HTTP; simulated Web Locks",
  "orion_device_test": false,
  "checks": [
    "Initial open: no history request, 24h selected, unified Sync & History expanded",
    "Explicit Collapse/Expand hides the large sync body while keeping the three usage cards visible",
    "24h completes first and a soft 2\u20135 minute stage wait is exposed with Start-now override",
    "Start-now override advances 7d \u2192 30d while widening coverage reads older pages beyond cached IDs",
    "Healthy completed baseline opens collapsed by default; manual expand/collapse is session-respected",
    "Panel reopen and new tab share saved baseline status without starting a scan",
    "Unchanged conversations: headers reconciled; zero extra message-detail fetches",
    "Passive completed reply capture persists metadata and deduplicates the same message ID",
    "429: one actual error, unrequested tasks remain pending/deferred, shared Retry-After cannot be bypassed",
    "After cooldown: resumes saved failed/pending tasks without deleting prior records",
    "Explicit Pause persists across scheduler ticks; no automatic restart",
    "A second tab sees the shared staged checkpoint and can continue after the first scanner releases the native lock",
    "Five Project HTTP 500s become a visible degraded warning and no longer block 24h core coverage",
    "Degraded Projects still allow staged progress; diagnostics include exportable error/log state",
    "Unknown list response blocks baseline instead of treating it as empty/complete",
    "Failed/ignored persistence cannot start an unverified history scan",
    "Promise-only WebExtension storage supports the same build as callback-only storage",
    "Partial v1.6 cache migrates as saved data awaiting Resume, never as a fresh empty baseline",
    "503 is retryable; repeated 404 is unavailable and remains an explicit coverage gap",
    "Existing v1.7.0 Project errors with 5+ attempts upgrade in place to non-blocking degraded warnings",
    "Fast backfill override is session-local and lowers request spacing without bypassing hard cooldowns",
    "Concurrent writes from two tabs are serialized without lost updates",
    "Account switch selects an isolated cache and updates detected plan rather than assuming Pro"
  ]
}
